<p id="notice"><%= notice %></p>

<table class="table">
	<tr>
		<th>Name:</th>
		<th>Workers</th>
	</tr>
	<tr>
	<td><%= @project.name %></td>
    <td><%= @project.workers.pluck(:name).join(', ') %></td>
	</tr>
</table>
  
  
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
